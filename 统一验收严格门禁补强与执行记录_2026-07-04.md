# 统一验收严格门禁补强与执行记录

生成时间：2026-07-04

## 1. 本次补强内容

### 1.1 统一验收标准补强

已在 `统一验收标准.md` 中新增“防漏验硬门禁”，覆盖：

- 总门禁结论规则；
- 用户可达性；
- 远端 master 覆盖；
- 前后端映射；
- 真实数据；
- 真实流程；
- 角色联动；
- AI 影像与模型能力；
- RAG/LLM/记忆；
- 视觉与交互；
- 反证检查；
- 验收脚本约束；
- UTF-8 编码；
- 不可替代证据清单。

核心约束：只要存在一个硬门禁未满足，最终结论不得写“通过/完成/可交付”。

### 1.2 医生端报告工作台入口补齐

问题：

- 头部 CT 检查报告工作台此前只能通过 `http://127.0.0.1:8030` 直接访问。
- 这不满足“用户必须从主平台角色界面自然点击到达”的硬门禁。

修复：

- 新增医生端路由：`/doctor/headct-reports`；
- 新增医生端页面：`frontend/src/views/doctor/HeadCtReportWorkbench.vue`；
- 医生首页新增入口：“CT 报告工作台”；
- 该页面以内嵌方式加载 `HeadCTReportService` 工作台；
- `HeadCTReportService/frontend/app.js` 支持读取主平台传入的 `actorId`、`actorRole`。

用户路径：

```text
医生登录 -> 医生工作台 -> 点击 CT 报告工作台 -> 进入头部 CT 检查报告工作台
```

截图证据：

```text
frontend/visual-results/full-project-ui-acceptance/167-DOCTOR_REPORT_WORKBENCH_REACHABILITY-医生端点击进入头部CT报告工作台.png
frontend/visual-results/full-project-ui-acceptance/155-doctor-headct-reports-路由截图.png
frontend/visual-results/full-project-ui-acceptance/156-doctor-headct-reports-页面主体与控件扫描.png
```

### 1.3 用户级验收脚本收紧

已修改 `frontend/tests/visual/full-project-ui-acceptance.spec.ts`：

- 不再直接 `goto('http://127.0.0.1:8030')` 作为报告工作台验收；
- 改为医生端点击“CT 报告工作台”后检查 iframe 内完整报告工作台；
- 不再把 `http://127.0.0.1:8000/ui` 作为用户级验收入口；
- 独立 CT 伪影 UI 只作为服务专项，不作为主平台用户入口。

已修改 `frontend/tests/visual/headct-real-user-workflow.spec.ts`：

- 上传 CT、AI 识别、报告生成仍从医生接诊页走；
- 报告草稿审核/签署/发布阶段改为从医生工作台点击进入报告工作台；
- `REPORT_BASE_URL` 仅保留为后端接口核验地址，不作为用户界面跳转。

## 2. 本次执行命令与结果

### 2.1 服务健康

```text
GET http://127.0.0.1:8092/actuator/health
Project2: UP
PostgreSQL: UP

GET http://127.0.0.1:8030/api/v1/health
HeadCTReportService: ok
Orchestrator: ok
Filter: ok
Lesion: ok
RAG vector_db: ok
EMR: ok
```

### 2.2 前端类型检查与构建

```text
cd frontend
npm run type-check
结果：PASS

npm run build-only
结果：PASS
```

### 2.3 全项目 UI 与交互验收

```text
cd frontend
npx playwright test -c playwright.config.ts tests/visual/full-project-ui-acceptance.spec.ts --project=desktop-chrome
结果：1 passed
耗时：3.3m
```

报告：

```text
全项目所有界面交互截图验收报告_2026-07-03.md
```

摘要：

```text
通过项：194
跳过项：67
失败项：0
截图目录：frontend/visual-results/full-project-ui-acceptance
```

说明：

- 跳过项仅限破坏性提交、文件选择框或需要特定真实业务状态的动作；
- 报告工作台已通过医生端入口点击进入，不再以手输独立服务 URL 作为证据。

### 2.4 头部 CT 真实用户链路

```text
cd frontend
npx playwright test -c playwright.config.ts tests/visual/headct-real-user-workflow.spec.ts --project=desktop-chrome
结果：1 passed
耗时：1.2m
```

用户流程：

```text
医生登录
-> 接诊页创建/选择检查申请
-> 上传真实样例 CT
-> AI 影像识别
-> 生成 AI 报告
-> 回到医生工作台
-> 点击 CT 报告工作台
-> 在报告工作台提交审核
-> 审核通过
-> 签署
-> 发布
-> EMR 归档核验
```

截图证据：

```text
frontend/visual-results/headct-real-user-workflow/desktop-chrome-01-doctor-selected-real-ct-file.png
frontend/visual-results/headct-real-user-workflow/desktop-chrome-02-upload-success.png
frontend/visual-results/headct-real-user-workflow/desktop-chrome-03-ai-recognition-with-visual-output.png
frontend/visual-results/headct-real-user-workflow/desktop-chrome-04-project2-ai-report-generated.png
frontend/visual-results/headct-real-user-workflow/desktop-chrome-05-report-workbench-draft.png
frontend/visual-results/headct-real-user-workflow/desktop-chrome-06-report-released-and-emr-archived.png
```

### 2.5 当前问题 20 轮点击复核

首次执行第 4 轮暴露脚本断言问题：

- 实际页面已进入“我的病历”；
- 旧断言只匹配“挂号记录/就诊记录/门诊”，未匹配真实页面标题；
- 已修正断言为 `我的病历|挂号记录|就诊记录|门诊`。

重新执行：

```text
python -X utf8 scripts\run_current_issues_20_rounds.py
```

结果：

```text
round 1 PASS
round 2 PASS
round 3 PASS
round 4 PASS
round 5 PASS
round 6 PASS
round 7 PASS
round 8 PASS
round 9 PASS
round 10 PASS
round 11 PASS
round 12 PASS
round 13 PASS
round 14 PASS
round 15 PASS
round 16 PASS
round 17 PASS
round 18 PASS
round 19 PASS
round 20 PASS
```

报告：

```text
目前存在问题_用户点击截图验收报告_2026-07-03.md
```

最后一轮截图目录：

```text
frontend/visual-results/current-issues-click-acceptance/round-20
```

### 2.6 Orchestrator/RAG 后端专项

```text
python -X utf8 -m pytest HeadCTOrchestrator\tests -q
```

结果：

```text
47 passed in 7.22s
```

## 3. 门禁对照结果

| 门禁 | 本次状态 | 证据 |
|---|---|---|
| 用户可达性 | 通过 | 医生端新增 CT 报告工作台入口；全项目 UI 验收截图 167 |
| 独立服务不可手输 URL 代替验收 | 通过 | 全项目验收脚本已移除报告工作台直接 `goto(8030)` |
| 前后端映射 | 部分通过 | 本次重点修复 ReportService 页面可达性；完整双向接口映射需继续由接口映射报告维护 |
| 真实数据/真实链路 | 通过本次关键链路 | HeadCT 真实用户链路上传 CT、AI识别、报告发布、EMR归档通过 |
| 角色联动 | 通过本次覆盖链路 | 患者挂号 20 轮、医生报告工作台、EMR 归档均有证据 |
| AI 影像可视化输出 | 通过本次链路 | `03-ai-recognition-with-visual-output.png` |
| RAG/LLM/记忆 | 通过本次后端专项 | `python -X utf8 -m pytest HeadCTOrchestrator\tests -q`：47 passed |
| 视觉与交互 | 通过本次桌面验收 | 全项目 UI 验收 194 PASS / 0 FAIL |
| 20轮复核 | 通过 | `run_current_issues_20_rounds.py` 20/20 PASS |
| UTF-8 输出 | 通过 | 本次 20 轮使用 `python -X utf8` |

## 4. 结论边界

本次已修复并验证此前暴露的关键问题：

- 统一验收标准已补成完整硬门禁；
- 医生端可以从主平台自然进入头部 CT 检查报告工作台；
- 头部 CT 真实链路不再通过手输报告服务 URL 完成用户界面验收；
- 全项目 UI 桌面验收、头部 CT 真实用户链路、当前问题 20 轮复核均通过。

仍需保持的长期门禁：

- 后续任何新增独立服务前端，都必须先接入主平台角色入口；
- 后续最终验收仍需逐条更新远端 master 覆盖对照表、前后端双向接口映射表、RAG/记忆专项证据；
- 若发现任一硬门禁未满足，最终结论必须降级为“不通过”或“部分通过但存在阻塞项”。
