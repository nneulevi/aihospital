# 最终用户视角验收记录（2026-06-30）

## 验收目标

从用户视角验证系统不是“仅接口能通”，而是在真实业务使用中满足：

- 主平台与头部 CT AI 链路端到端可用。
- 患者、医生、管理员、医技、药房、移动患者端界面无红色错误提示。
- 页面跳转、角色登录、业务数据展示、状态流转一致。
- 头部 CT 流程具备稳定可视化输出：上传 CT、AI 识别、AI 辅助结论、报告发布、EMR 归档。

## 本轮修复

1. 统一 Project2 本地数据库配置入口：
   - 新增 `scripts/project2_db_env.py`，供 E2E/字段一致性脚本读取同一套 Project2 DB 配置。
   - 新增 `scripts/project2_env.example.ps1`，本机实际口令放在被 git 忽略的 `scripts/project2_env.local.ps1`。
   - 更新 `scripts/start_headct_platform.ps1`，启动脚本会优先读取本地 Project2 env。

2. 修复视觉验收环境问题：
   - `frontend/playwright.config.ts` 将 Playwright 输出目录移到 `.tmp/playwright`，避免 OneDrive/云同步属性影响 `test-results`。
   - `frontend/vite.config.ts` 将 Vite cache 移到 `.tmp/vite-cache/frontend`，避免 `node_modules/.vite` 旧缓存文件被 Windows 云同步属性锁住。

3. 修复 HeadCT 截图级验收 spec：
   - 重写 `frontend/tests/visual/headct-workflow-screenshots.spec.ts` 为干净 UTF-8。
   - 断言真实用户可见的中文文案和结构化状态，不再依赖损坏编码或固定标点。

## 启动验证

命令：

```powershell
.\scripts\start_headct_platform.ps1 -Reload
```

结果：

- filter already running: `http://127.0.0.1:8000/api/ct-artifact/health`
- lesion already running: `http://127.0.0.1:8021/api/head-ct-lesion/health`
- orchestrator already running: `http://127.0.0.1:8010/api/head-ct-ai/health`
- emr already running: `http://127.0.0.1:8040/api/v1/health`
- report already running: `http://127.0.0.1:8030/api/v1/health`
- project2 already running: `http://127.0.0.1:8092/actuator/health`

Project2 health：

```json
{
  "status": "UP",
  "components": {
    "db": {
      "status": "UP",
      "details": {
        "database": "PostgreSQL",
        "validationQuery": "isValid()"
      }
    }
  }
}
```

## 逻辑验收

### Project2 核心业务

命令：

```powershell
python .\scripts\e2e_project2_core_business.py
```

结果：`status = success`

覆盖：

- 患者挂号。
- 医生接诊。
- 保存病历。
- 开立检查、检验、处置、处方。
- 患者缴费。
- 药房发药、退药。
- 挂号取消。
- 管理员、医生、患者 dashboard 数据一致。

### 医技 / 药房 / 排班号源扩展业务

命令：

```powershell
python .\scripts\e2e_project2_extended_business.py
```

结果：`status = success`

覆盖：

- 排班号源新增、调整号额、停诊、恢复。
- 医技任务收费后执行、报告录入、AI 解读。
- 药品库存入库、盘点、库存预警、发药、退药。
- 药品库存流水 IN / CHECK / DISPENSE / REFUND 状态一致。

### 头部 CT 正常业务链路

命令：

```powershell
python .\scripts\e2e_project2_real_business.py
```

结果：`status = success`

关键结果：

- `project2 = UP`
- `orchestrator = ok`
- `report = ok`
- `emr = ok`
- `report_service_status = released`
- `emr_status = final`
- `ai_image_analysis > 0`
- `ai_generated_report > 0`

覆盖：

- Project2 创建真实就诊与 CT 检查上下文。
- AI 问诊和 AI 辅助诊断建议生成。
- Project2 上传头部 CT 文件。
- Project2 调 Orchestrator。
- Orchestrator 调 Filter / Lesion / RAG-LLM。
- Project2 生成报告。
- ReportService 发布报告。
- EMRService 完成归档。

### 字段一致性

命令：

```powershell
python .\scripts\check_project2_field_consistency.py
```

结果：

```text
Project2 field consistency check passed
```

覆盖：

- 前端渲染依赖的药品编码、规格、单位、价格、厂家等字段非空。
- 患者端记录/待缴费计数与列表一致。
- 医生端队列、患者详情、检查结果字段完整。
- 管理员、医技、药房页面关键字段结构一致。

## 截图级视觉验收

命令：

```powershell
cd frontend
$env:ESBUILD_BINARY_PATH='D:\exam\.tmp\bin\esbuild.exe'
npx playwright test -c playwright.config.ts
```

结果：

```text
22 passed
2 skipped
```

说明：

- `headct-workflow-screenshots.spec.ts` 只在 desktop 项目采集一次完整 HeadCT 证据，因此 mobile 中跳过是预期行为。
- `mini-patient.spec.ts` 只在 mobile 项目采集移动患者端证据，因此 desktop 中跳过是预期行为。

已覆盖页面：

- 登录入口：患者登录、医护/管理员登录。
- 患者端：AI 问诊、预约挂号、挂号成功、电子病历、门诊缴费、个人中心。
- 医生端：医生首页、患者接诊工作台、医生个人信息。
- 管理员端：首页、排班生成、号源管理、财务管理、药品管理。
- 医技端：任务工作台。
- 药房端：药房库存与发药工作台。
- 移动患者端：首页、AI、预约挂号、记录、缴费、个人中心。
- 头部 CT：上传 CT 图像、病灶识别、AI 辅助结论、报告发布、EMR 归档。

HeadCT 关键截图证据：

- `frontend/visual-results/headct-workflow/desktop-chrome-01-upload-ct-image.png`
- `frontend/visual-results/headct-workflow/desktop-chrome-02-lesion-recognition.png`
- `frontend/visual-results/headct-workflow/desktop-chrome-03-ai-assisted-conclusion-and-report.png`
- `frontend/visual-results/headct-workflow/desktop-chrome-04-report-released.png`
- `frontend/visual-results/headct-workflow/desktop-chrome-05-emr-archive.png`

## 前端类型检查

命令：

```powershell
cd frontend
npm run type-check
```

结果：通过。

## 结论

本轮按用户视角完成最终验收：

- 所有核心服务可由统一脚本识别为 ready。
- Project2 数据库健康状态为 UP。
- 主平台核心业务、扩展业务、头部 CT AI 正常链路均端到端通过。
- 前端桌面端和移动端截图级验收通过，无红色错误提示、无 500 响应、无页面运行时错误。
- 头部 CT 流程具备稳定可视化证据，可看到上传、识别、AI 结论、报告发布和 EMR 归档。

