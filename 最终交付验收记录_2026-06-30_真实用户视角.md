# 最终交付验收记录（2026-06-30，真实用户视角）

## 验收原则

- 不再以旧的 API-only 脚本作为最终证据。
- 使用 Playwright 从用户视角访问页面，验证登录后的医生端、患者端、管理员端、医技端、药房端、患者移动端入口。
- 头部 CT AI 流程使用真实上传文件 `testdata/headct/head_ct_positive_case.nii.gz`，不使用 smoke 脚本代替上传。
- 所有 Python 验证命令使用 `python -X utf8` 或显式 UTF-8 环境变量；启动脚本已设置 `PYTHONUTF8=1`、`PYTHONIOENCODING=utf-8`。

## 服务状态

已启动并通过健康检查：

- Filter：`http://127.0.0.1:8000/api/ct-artifact/health`
- Lesion：`http://127.0.0.1:8021/api/head-ct-lesion/health`
- Orchestrator：`http://127.0.0.1:8010/api/head-ct-ai/health`
- Report：`http://127.0.0.1:8030/api/v1/health`
- EMR：`http://127.0.0.1:8040/api/v1/health`
- Project2：`http://127.0.0.1:8092/actuator/health`

当前 AI 权重状态：

- 金属伪影模块：使用 `Filter/model/runs/metal_unet3d/best_unet3d_metal.pt`，`checkpoint_fallback_used=false`。
- 病灶识别模块：使用 VinBigData MIDL2020 raw ResNet50 权重 `HeadCTLesionDetection/models/hemorrhage/external_weights/best_resnet50.pth`，`checkpoint_fallback_used=false`。
- 说明：公开成熟的金属伪影分割 checkpoint 当前仍未提供，InDuDoNet 已登记为金属伪影校正候选权重，但当前链路尚未执行 MAR 图像校正。页面中已明确显示该限制，不再伪装为已校正。

## 已执行验证

### 1. 编译与类型检查

- `npm run type-check`
- `cmd /c ".\mvnw.cmd -q -DskipTests -Dmaven.test.skip=true compile -Dmaven.repo.local=D:\exam\.m2\repository"`

结果：均通过。

### 2. 头部 CT AI 到报告/EMR 真实用户流程

命令：

```powershell
$env:PYTHONUTF8='1'
$env:PYTHONIOENCODING='utf-8'
$env:PLAYWRIGHT_ARTIFACT_ROOT='D:\exam\.tmp\playwright-real-user'
npx playwright test tests/visual/headct-real-user-workflow.spec.ts --project=desktop-chrome -c playwright.config.ts
```

结果：`1 passed`。

覆盖流程：

1. 医生身份登录。
2. 选择就诊中患者。
3. 新建头颅 CT 检查申请。
4. 在医生工作站上传真实 CT 文件。
5. 执行 AI 影像识别。
6. 页面显示 AI 置信度、影像所见、AI 结论、三视图可视化输出、链路状态。
7. 生成报告。
8. 打开报告工作台，完成提交审核、审核通过、签署、发布。
9. 触发 EMR 归档。
10. 验证 EMR 诊断报告状态为 `final`。

截图证据：

- `frontend/visual-results/headct-real-user-workflow/desktop-chrome-01-doctor-selected-real-ct-file.png`
- `frontend/visual-results/headct-real-user-workflow/desktop-chrome-02-upload-success.png`
- `frontend/visual-results/headct-real-user-workflow/desktop-chrome-03-ai-recognition-with-visual-output.png`
- `frontend/visual-results/headct-real-user-workflow/desktop-chrome-04-project2-ai-report-generated.png`
- `frontend/visual-results/headct-real-user-workflow/desktop-chrome-05-report-workbench-draft.png`
- `frontend/visual-results/headct-real-user-workflow/desktop-chrome-06-report-released-and-emr-archived.png`

### 3. Project2 桌面端用户视角验收

命令：

```powershell
$env:PYTHONUTF8='1'
$env:PYTHONIOENCODING='utf-8'
$env:PLAYWRIGHT_ARTIFACT_ROOT='D:\exam\.tmp\playwright-project2-user'
npx playwright test tests/visual/project2-visual.spec.ts tests/visual/project2-full-acceptance.spec.ts --project=desktop-chrome -c playwright.config.ts
```

结果：`11 passed`。

覆盖页面：

- 登录入口。
- 患者端：AI 问诊、预约挂号、预约成功、就诊记录、待缴费、个人中心。
- 医生端：首页、个人中心、患者接诊详情。
- 管理员端：首页、排班、号源、财务、药品库存。
- 医技端：检查任务与报告录入。
- 药房端：处方核对、发药、库存信息。

### 4. 患者移动端小程序入口验收

命令：

```powershell
$env:PYTHONUTF8='1'
$env:PYTHONIOENCODING='utf-8'
$env:PLAYWRIGHT_ARTIFACT_ROOT='D:\exam\.tmp\playwright-mini-patient'
npx playwright test tests/visual/mini-patient.spec.ts --project=mobile-chrome -c playwright.config.ts
```

结果：`1 passed`。

覆盖页面：

- 移动端患者首页。
- 移动端 AI 问诊。
- 移动端预约挂号。
- 移动端就诊记录。
- 移动端待缴费。
- 移动端个人中心。

## 本次修复点

- Project2 AI 影像分析响应新增 `previewUrls`，医生端可直接显示 Filter 输出的轴位、冠状位、矢状位图像。
- 医生端患者接诊页新增“影像可视化输出”，避免只有文字结论、缺少影像证据。
- 报告工作台展示 EMR 外部文档编号，发布归档后可在页面确认已入 EMR。
- Playwright 真实用户流程截图前等待临时提示消失，避免 toast 遮挡最终验收截图。
- 启动脚本统一设置 UTF-8 环境，降低 PowerShell/Windows 控制台造成的中文乱码风险。
- 旧的 `headct-workflow-screenshots.spec.ts` 已标记为 legacy skip，避免继续用 `scripts/e2e_project2_real_business.py` 作为最终验收依据。

## 当前可交付结论

当前版本满足“规范项目演示与业务闭环可用”的交付标准：

- 全部核心服务可启动。
- Web 端与移动端患者入口可访问。
- 登录后角色界面按患者、医生、管理员、医技、药房区分。
- 头部 CT 流程能从真实 CT 上传进入 AI 分析，并形成可视化结果、报告审核发布、EMR 归档。
- 页面验收未发现红色错误提示、500 响应、明显跳转异常或字段空挂问题。

仍需如实保留的模型限制：

- 金属伪影校正 InDuDoNet 尚未执行实际 MAR 校正。
- 病灶模型当前为分类输出，不是三维病灶分割。
- 若要宣称医学模型生产级精度，仍需要正式数据集评估、外部验证集、模型版本冻结和阈值校准；本记录仅证明软件链路与用户体验达到当前项目交付要求。
