# 三端联动核查记录（2026-07-03）

## 核查结论

患者端、医生端、管理员端可以联动。已通过脚本验证患者挂号、报道、排队、医生接诊、检查检验处置、处方、收费、药房库存、医技结果、财务统计、头部 CT AI、报告审核发布、EMR 归档等跨模块状态同步。

## 运行环境

| 服务 | 状态 |
|---|---|
| Filter 金属伪影服务 | ok |
| Lesion 病灶识别服务 | ok，`mode=model` |
| Orchestrator 头部 CT 编排服务 | ok |
| Report 服务 | ok |
| EMR 服务 | ok |
| Project2 主平台 | UP |

## 已执行验收

### 1. 字段与接口映射

命令：

```powershell
$env:PYTHONIOENCODING='utf-8'
python scripts\check_project2_field_consistency.py
python scripts\check_project2_strict_acceptance_mapping.py
```

结果：

- `Project2 field consistency check passed`
- `Strict Project2 frontend/backend mapping check passed.`

说明：

- 前后端字段映射通过。
- 后端接口与前端暴露功能映射通过。
- 未发现药品库存字段缺失、空字段展示等同类字段错配问题。

### 2. 核心三端联动

命令：

```powershell
python scripts\e2e_project2_core_business.py
```

结果：`status=success`

关键证据：

- 患者挂号后，医生列表与待诊患者队列可见对应记录。
- 患者报道后，患者端排队状态更新为 `CHECKED_IN`。
- 医生接诊后，患者端最新就诊状态进入后续流程。
- 医生开立检查、检验、处置、处方后，患者端待缴费数量与金额同步。
- 诊断完成后，患者端就诊记录显示 `DIAGNOSIS_DONE`。
- 管理员 dashboard 统计包含今日挂号、待收费金额、库存预警、待审核报告等跨模块数据。

本次输出摘要：

| 项 | 结果 |
|---|---|
| `waiting_patient_count` | 1 |
| `orders_count` | 4 |
| 医生端 `finishedToday` | 1 |
| 患者端 `unpaidOrderCount` | 3 |
| 患者端 `unpaidAmount` | 240.0 |
| 患者最终状态 | `DIAGNOSIS_DONE` |

### 3. 医技、药房、库存、财务联动

命令：

```powershell
python scripts\e2e_project2_extended_business.py
```

结果：`status=success`

关键证据：

- 医技任务从收费后进入任务列表。
- 检查、检验、处置报告录入后，医生端可以读取结果。
- 药品库存字段完整：编码、名称、规格、单位、价格、厂家、库存、预警状态。
- 库存流水完整：
  - `IN`：8 -> 28
  - `CHECK`：28 -> 25
  - `DISPENSE`：25 -> 23
  - `REFUND`：23 -> 25

### 4. 用户逻辑综合验收

命令：

```powershell
python scripts\e2e_user_logic_acceptance.py
```

结果：`status=success`

覆盖角色：

- 医生：`doctor`
- 管理员：`admin`
- 医技人员：`medicaltech`
- 药师：`pharmacist`
- 患者：`13800001111`

关键证据：

| 阶段 | 患者端统计 |
|---|---|
| 挂号后 | `recordCount=1`, `unpaidOrderCount=0`, `latestVisitState=REGISTERED` |
| 医生开立未缴费项目后 | `unpaidOrderCount=4`, `unpaidAmount=265.0`, `latestVisitState=DOCTOR_RECEIVED` |
| 医技项目收费后 | `unpaidOrderCount=1`, `unpaidAmount=25.0` |
| 全部收费后 | `unpaidOrderCount=0`, `unpaidAmount=0` |
| 最终病历 | `visitState=DIAGNOSIS_DONE`, `visitStateName=已确诊` |

说明：

- 已复核此前出现过的“我的页面显示待缴费数量，但缴费页无项目”的同类问题。
- 当前患者统计与待缴费状态能随真实业务动作同步变化。
- 医技任务、药房库存、财务收费/退款、患者报告均有联动证据。

### 5. 头部 CT AI、报告、EMR 联动

命令：

```powershell
python scripts\e2e_project2_real_business.py
```

结果：`status=success`

关键证据：

| 项 | 结果 |
|---|---|
| Project2 | `UP` |
| Orchestrator | `ok` |
| Report | `ok` |
| EMR | `ok` |
| AI 问诊记录 | `ai_consultation=1` |
| AI 辅助诊断建议 | `ai_diagnosis_suggestion=3` |
| AI 影像分析 | `ai_image_analysis=1` |
| AI 报告记录 | `ai_generated_report=1` |
| 影像分析状态 | `ready_for_project_demo` |
| 工作流状态 | `ai_imaging_workflow_ready=true` |
| 报告服务状态 | `released` |
| 报告版本 | `2` |
| EMR 状态 | `final` |

说明：

- Project2 能创建头部 CT 相关业务病例。
- AI 问诊、AI 辅助诊断、影像分析、报告生成、医生审核发布、EMR 归档可以串联。
- 报告服务发布后，EMR 文档状态为 `final`。

## 仍需注意

1. 当前脚本验证的是后端与业务状态联动；若要证明所有页面视觉无误，还需继续执行：

```powershell
cd D:\exam\frontend
npm run test:visual
```

2. 头部 CT AI 已使用真实模型权重和真实影像链路，但 CQ500 官方标签缺失时，仍不能给出监督 AUC/敏感度/特异度结论。

3. 若修改数据库种子、前端字段或接口 DTO，应重新运行本文档中的全部验收命令。

## 总结

从本次命令输出看，患者、医生、管理员三端不是孤立页面，而是围绕同一套后端业务状态联动：

- 患者正常挂号后，医生端能看到并接诊。
- 医生开立项目后，患者端待缴费同步变化。
- 收费后，医技/药房端出现对应任务。
- 医技报告和处方状态能回流医生端、患者端。
- 管理员端统计和库存/财务数据随业务动作更新。
- 头部 CT AI 结果能进入报告服务并归档到 EMR。
