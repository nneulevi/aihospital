# Optional medical imaging training stack.
# Install this when using MONAI training or nnU-Net segmentation workflows.

-r requirements.txt
monai>=1.3

# Windows + Python 3.9 compatibility notes:
# - python-gdcm 3.2.x has no usable cp39 Windows wheel here, so pip falls
#   back to a source package that fails during CMake metadata generation.
# - dicom2nifti 2.6.x imports pydicom.pixels, which requires pydicom 3.x;
#   pydicom 3.x requires Python >=3.10.
# The following versions were installed and import-tested together.
python-gdcm==3.0.26
pydicom==2.4.4
dicom2nifti==2.5.1
blosc2==2.5.1
nnunetv2==2.5.2
