# AI影像模型能力补强记录（2026-07-03）

## 目标

补齐“AI 影像识别效果较好、模型能力足够强”这一步，要求不再用 smoke 或报告话术替代模型能力验证。

## 已完成

### 1. 成熟公开权重接入状态

- 颅内出血分类：已接入 VinBigData MIDL2020 CNN-LSTM ICH 公开权重 `best_resnet50.pth`。
- 本地验证命令：`python scripts\validate_true_checkpoints.py --strict-mature`
- 验证结果：
  - `lesion_hemorrhage_classification.selection.selected_provenance = mature_public_external_raw`
  - `lesion_hemorrhage_classification.selection.fallback_used = false`
  - 权重路径：`HeadCTLesionDetection\models\hemorrhage\external_weights\best_resnet50.pth`

### 2. 真实 CQ500 DICOM 子集转换

- 输入数据：`D:\Data`
- 输出 manifest：`HeadCTLesionDetection\datasets\cq500ct200_subset\manifest.csv`
- 输出 NIfTI：`HeadCTLesionDetection\datasets\cq500ct200_subset\images`
- 本次转换：12 例真实 DICOM，12 例成功转换。
- 标签状态：本地未发现官方 `reads.csv` / `prediction_probabilities.csv`，因此当前子集 `labeled=0`。

### 3. 真实权重真实影像推理

- 推理脚本：`HeadCTLesionDetection\models\hemorrhage\run_manifest_inference.py`
- 权重：`HeadCTLesionDetection\models\hemorrhage\external_weights\best_resnet50.pth`
- 输出：`HeadCTLesionDetection\models\hemorrhage\runs\vinbigdata_cq500_calibration\predictions.jsonl`
- 本次推理：6 例真实 CQ500 NIfTI。
- 结果确认：
  - `checkpoint_framework = state_dict`
  - `provider = vinbigdata`
  - 未触发本地 fallback。

### 4. 校准与置信带

新增：

- `HeadCTLesionDetection\models\hemorrhage\calibration.py`
- `HeadCTLesionDetection\models\hemorrhage\calibrate_predictions.py`
- `HeadCTLesionDetection\models\hemorrhage\prepare_cq500_reads_labels.py`

能力：

- 支持监督校准：若提供官方/人工标签，输出 `recommended_threshold`、AUC、敏感度、特异度、F1 等指标。
- 支持无监督分布校准：若没有标签，仅输出概率分布阈值，并明确标注不能作为准确率证据。
- 病灶服务输出新增：
  - `decision_threshold`
  - `confidence_band`
  - `calibration`
  - 边界概率提示语

本次生成的无监督校准文件：

`HeadCTLesionDetection\models\hemorrhage\runs\vinbigdata_cq500_calibration\calibration.json`

注意：该文件 `source = cq500ct200_unsupervised_distribution`，启动脚本默认不会将其作为生产阈值使用。只有设置：

```powershell
$env:VINBIGDATA_ALLOW_UNSUPERVISED_CALIBRATION="true"
```

才会显式启用。

### 5. 官方 CQ500 reads.csv 接入器

新增脚本：

```powershell
python HeadCTLesionDetection\models\hemorrhage\prepare_cq500_reads_labels.py `
  --manifest HeadCTLesionDetection\datasets\cq500ct200_subset\manifest.csv `
  --reads-csv <CQ500官方reads.csv路径> `
  --output-labels-csv HeadCTLesionDetection\datasets\cq500ct200_subset\labels.csv
```

特性：

- 支持 `CQ500-CT-200` 与 `CQ500CT200` 编号归一化。
- 支持三阅片者多数投票。
- 自动生成项目训练/评估兼容的 `labels.csv`。

## 验收命令

```powershell
$env:PYTHONIOENCODING='utf-8'
python -m pytest HeadCTLesionDetection\tests -q
python scripts\validate_true_checkpoints.py --strict-mature
```

本次结果：

- `HeadCTLesionDetection\tests`：18 passed。
- `validate_true_checkpoints.py --strict-mature`：通过。

## 仍未完全完成的部分

当前不能宣称“模型能力已经充分强”，原因：

1. 本地 `D:\Data` 中未发现官方 CQ500 标签 CSV，无法计算监督 AUC、敏感度、特异度。
2. 出血分割成熟公开 checkpoint 尚未接入；当前成熟公开能力主要是出血分类。
3. 本次 CQ500 子集推理结果多数为低概率阴性，需用官方标签判断这是正确阴性还是漏检。

## 下一步

1. 获取 CQ500 官方 `reads.csv`。
2. 使用 `prepare_cq500_reads_labels.py` 生成 `labels.csv`。
3. 运行监督评估：

```powershell
python HeadCTLesionDetection\models\hemorrhage\evaluate.py `
  --labels-csv HeadCTLesionDetection\datasets\cq500ct200_subset\labels.csv `
  --checkpoint HeadCTLesionDetection\models\hemorrhage\external_weights\best_resnet50.pth `
  --provider vinbigdata `
  --device cpu `
  --output-dir HeadCTLesionDetection\models\hemorrhage\runs\vinbigdata_cq500_supervised_eval `
  --threshold 0.5 `
  --calibrate-threshold `
  --vinbigdata-image-size 224 `
  --vinbigdata-max-slices 48
```

4. 若监督指标达标，再将监督校准文件复制或输出到：

`HeadCTLesionDetection\models\hemorrhage\runs\vinbigdata_cq500_calibration\calibration.json`

此时启动脚本会自动应用该监督阈值。
