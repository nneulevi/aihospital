# 前端模块衔接与验收记录

## 本轮目标

依据 `前端模块衔接与验收指导.md`，优先处理仍使用模拟 API 的 P0 页面，并恢复前端类型检查与生产构建能力。

## 已完成内容

### 1. 患者病历详情

文件：`src/views/patient/RecordDetail.vue`

完成情况：

- 移除 `mockGetRecordDetail`。
- 接入真实接口 `getRecordDetail`。
- 增加加载态、空状态、接口失败提示。
- 保留“重新问诊”“再次挂号”入口。

### 2. 管理端 AI 排班

文件：`src/views/admin/AISchedule.vue`

完成情况：

- 移除 `mockScheduleGenerate`。
- 接入真实接口 `scheduleGenerate`。
- 保留科室、开始日期、结束日期、规则配置输入。
- 展示真实返回的 `DailySchedule[]` 排班结果。

### 3. 管理端财务收费

文件：`src/views/admin/FinanceManage.vue`

完成情况：

- 移除收费、退费、记录、日结的模拟 API。
- 接入真实接口：
  - `charge`
  - `refund`
  - `getFinanceRecords`
  - `getDailySummary`
- 收费/退费改为真实业务表单：挂号 ID、项目 ID 列表、金额、支付方式/原因。
- 收费或退费成功后自动刷新收费记录。

### 4. 管理端药房

文件：`src/views/admin/DrugManage.vue`

完成情况：

- 移除库存、发药、退药模拟 API。
- 接入真实接口：
  - `getDrugInventory`
  - `dispense`
  - `drugRefund`
- 发药/退药改为真实处方 ID、药师 ID、退药原因输入。
- 发药或退药成功后自动刷新库存。

### 5. 基础构建修复

完成情况：

- 修复 `src/utils/request.ts` 中损坏的字符串字面量，并统一转为 UTF-8。
- 调整 `tsconfig.node.json`，去除当前 TypeScript 不兼容的 `@tsconfig/node22` 扩展。
- 为 `tsconfig.app.json`、`tsconfig.node.json` 增加 `skipLibCheck`，避免第三方包可选构建器类型阻断项目验收。
- 修复 `src/api/index.ts` 包装层类型，使 Orval API 的兼容返回结构可被页面继续使用。
- 为患者登录补充 `patientSendCode` 调用适配。
- 修复若干旧页面的 Vant `van-tag size="small"` 类型不兼容问题。
- 对订单页、个人中心做局部前端类型兼容，不修改 Orval 生成模型。

### 6. 路由守卫与登录回跳

文件：

- `src/router/index.ts`
- `src/views/patient/Login.vue`
- `src/views/auth/StaffLogin.vue`

完成情况：

- 恢复 token 与角色校验。
- 患者端、医生端、管理员端按 `PATIENT / DOCTOR / ADMIN` 进行访问控制。
- 未登录访问受保护页面时跳转到对应登录页，并保留 `redirect`。
- 登录成功后优先回跳到原访问页面。

### 7. 医生端 AI 影像识别与报告生成入口

文件：`src/views/doctor/PatientVisit.vue`

完成情况：

- 在“结果”Tab 中补充 AI 影像识别与报告生成区域。
- 接入真实接口：
  - `upload`
  - `analyze`
  - `reportGenerate`
- 支持选择影像文件、上传影像、填写/自动带入检查申请 ID、展示影像分析所见、结论、置信度、标注框和 AI 报告内容。

### 8. 模拟 API 清理确认

检查命令：

```powershell
Select-String -Path 'src/views/**/*.vue','src/router/index.ts','src/api/index.ts' -Pattern 'mock|模拟|mockGet|mock[A-Z]'
```

结果：未发现剩余模拟 API 调用。

## 验收命令

### 类型检查

```powershell
cd D:\exam\frontend
npm run type-check
```

结果：通过。

### 模拟 API 扫描

```powershell
cd D:\exam\frontend
Select-String -Path 'src/views/**/*.vue','src/router/index.ts','src/api/index.ts' -Pattern 'mock|模拟|mockGet|mock[A-Z]'
```

结果：未发现剩余模拟 API 调用。

### 本地开发服务与浏览器验证

```powershell
cd D:\exam\frontend
npm run dev -- --host 0.0.0.0 --port 5173
```

结果：`http://localhost:5173` 返回 200。

浏览器只读验证：

- 访问 `http://localhost:5173/doctor`，自动跳转到 `/auth/login?redirect=/doctor`，员工登录页正常渲染。
- 访问 `http://localhost:5173/patient/ai`，自动跳转到 `/patient/login?redirect=/patient/ai`，患者登录页正常渲染。
- 登录页可见文案正常，无明显乱码。

### Project2 与头部 CT AI 联调验证

后端启动：

```powershell
cd D:\exam
powershell -ExecutionPolicy Bypass -File .\scripts\start_headct_platform.ps1
```

健康检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8092/actuator/health
```

结果：Project2 `status=UP`，数据库组件 `status=UP`。

头部 CT AI 平台 smoke 测试：

```powershell
python scripts\smoke_test_headct_platform.py
```

结果摘要：

- `orchestrator_status=success`
- `filter_backend=unet3d`
- `lesion_status=success`
- `rag_status=success`
- `llm_status=success`
- 报告状态 `released`
- EMR 状态 `final`

Project2 真实业务链路测试：

```powershell
python scripts\e2e_project2_real_business.py
```

结果摘要：

- `status=success`
- Project2 / Orchestrator / Report / EMR 健康状态正常
- 生成患者、挂号、病历、检查申请业务数据
- AI问诊推荐数：`2`
- AI辅助诊断建议数：`1`
- AI影像分析写入：`1`
- AI生成报告写入：`1`
- 报告服务状态：`released`
- EMR 状态：`final`

### 生产构建

```powershell
cd D:\exam\frontend
npm run build
```

结果：通过。

说明：

- 沙箱内曾出现 `esbuild spawn EPERM`，已在非沙箱环境完成完整构建验证。
- 构建存在 Rollup 对第三方注释的提示，以及大 chunk 提示，不影响本轮功能验收。

## 当前仍需真实业务环境验证

以下内容需要 Project2 后端和数据库中存在真实业务数据后，在浏览器中进行端到端确认：

1. `/patient/record/:id` 是否能正确展示真实病历详情。
2. `/admin/schedule` 调用 `scheduleGenerate` 后，后端是否落库并返回完整排班结果。
3. `/admin/finance` 使用真实挂号 ID、项目 ID 完成收费/退费。
4. `/admin/finance` 能查询真实收费记录和日结统计。
5. `/admin/drug` 能查询真实库存，并完成真实处方发药/退药。
6. `/doctor/visit` 能上传真实影像文件，调用 AI 影像识别，并生成 AI 报告。
7. 未登录访问患者、医生、管理员页面时，能被正确拦截并在登录后回跳。

## 后续建议

1. 使用真实 Project2 账号和业务数据完成浏览器端逐项验收。
2. 对真实业务验收截图、接口响应和数据库落库证据做归档。
3. 对 Vite 构建大 chunk 增加 `manualChunks` 优化。
4. 若后端 OpenAPI 变化，重新生成 Orval 客户端并补充对应页面适配。

## 2026-06-24 前后端功能逐项映射核查

新增核查报告：`frontend/前后端功能映射核查报告.md`。

核查结论：

- 以当前 Project2 OpenAPI 为准，共核查后端接口 `33` 个。
- 前端页面映射结果：`33 / 33`。
- 未映射缺口：`0`。
- 查询型、分析型、报告型接口均有前端展示区域。
- 动作型接口均有按钮入口、确认弹窗、Toast、状态刷新或页面跳转反馈。

本次补齐：

- 医生待诊队列接入 `receivePatient`，点击待诊患者时先接诊再进入诊疗页。
- 医生诊疗页接入 `getPatientDetail`，展示真实患者信息。
- AI排班页接入 `getResults`，展示历史排班结果。
- 患者病历/挂号记录接入 `cancelRegister`，支持取消待就诊挂号。
- 药品库存页接入 `getInventory`，支持药房通用库存查询。

验证结果：

- `npm run type-check`：通过。
- `npm run build`：通过。
- `python scripts\e2e_project2_real_business.py`：通过，业务链路状态 `success`。
